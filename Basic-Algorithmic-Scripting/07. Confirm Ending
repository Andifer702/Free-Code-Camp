function confirmEnding(str, target) {
//match the letters of str and target from the end to see if it is the same
  for (var i = 1; i <= target.length; i++){
    if (str.substr(-i, i) != target.substr(-i, i)){
      return false;
    }
  }
  return true;
}
