function titleCase(str) {
  var stripStr = str.toLowerCase().split(" ");
  
  for (var i = 0; i < stripStr.length; i++){
   stripStr[i] = stripStr[i].replace(stripStr[i].charAt(0), stripStr[i].charAt(0).toUpperCase());
  }
  
  return stripStr.join(" ");
}
